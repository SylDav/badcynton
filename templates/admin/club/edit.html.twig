{% extends 'base.html.twig' %}
{% block title 'Éditer le club' %}

{% block body %}

    <div class="container mt-4">
        <h1>Éditer le club "{{ club.name }}"</h1>
        {{ include ('admin/club/_form.html.twig', {button: 'Éditer'}) }}
    </div>
{% endblock %}
