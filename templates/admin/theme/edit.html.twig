{% extends 'base.html.twig' %}
{% block title 'Éditer le thème' %}

{% block body %}

    <div class="container mt-4">
        <h1>Éditer le thème "{{ theme.name }}"</h1>
        {{ include ('admin/theme/_form.html.twig', {button: 'Éditer'}) }}
    </div>
{% endblock %}
